{
  "name": "orbo-e2e",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "End-to-end tests for Orbo to verify the SSR + Lazy Hydration behavior works in a real browser environment",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.2",
    "http-server": "^14.1.1"
  }
}
